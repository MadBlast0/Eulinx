/**
 * P18-UI-DASH — Re-export of the theme hook (Themes-Part01).
 *
 * Kept importable as `@/ui/themes` so surfaces do not reach into the
 * tokens folder directly.
 */

export { useTheme, type UseThemeValue } from "@/ui/tokens/theme-provider"
