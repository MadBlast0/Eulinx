{
  "identifier": "default",
  "description": "Default capabilities granted to the frontend",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "fs:default",
    "dialog:default",
    "store:default"
  ]
}
